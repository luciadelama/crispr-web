services:
  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: /api
    image: crispr-frontend
    expose:
      - "80"
    networks:
      - crispr
    depends_on:
      - backend

  admin:
    build:
      context: ./admin
      args:
        VITE_API_URL: /api
    image: crispr-admin
    expose:
      - "80"
    networks:
      - crispr
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    image: crispr-backend
    expose:
      - "4000"
    env_file:
      - ./backend/.env.production
    networks:
      - crispr

networks:
  crispr:
    driver: bridge